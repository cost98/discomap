# Minimal PostgreSQL configuration for DiscoMap
# Only essential settings for large hypertables

# TimescaleDB extension (REQUIRED)
shared_preload_libraries = 'timescaledb'

# Network
listen_addresses = '*'

# Increase locks for large number of chunks
max_locks_per_transaction = 512
