# Production Environment Configuration
# Copy this file to .env.production and customize

# === Database Configuration ===
POSTGRES_PASSWORD=changeme_to_strong_password
POSTGRES_PORT=5432

# === API Configuration ===
API_PORT=8000

# === Grafana Configuration ===
GRAFANA_ADMIN_PASSWORD=changeme_to_strong_password
GRAFANA_PORT=3000

# === pgAdmin Configuration (Optional) ===
PGADMIN_EMAIL=admin@yourdomain.com
PGADMIN_PASSWORD=changeme_to_strong_password
PGADMIN_PORT=5050

# === Timezone ===
TZ=Europe/Rome
